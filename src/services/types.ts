export type QueryTypes = {
  category: number;
  difficulty: string;
};
export type allQueryTypes = {
  amount: number;
  category: number;
  difficulty: string;
  token: string;
};

export type QuestionsCountTypes = {
  categoryId: number;
  difficulty: string;
};
