export type QueryTypes = {
  category: number;
  difficulty: string;
  token: string;
};
export type QuesriesWithAmountTypes = {
  amount: number;
  category: number;
  difficulty: string;
  token: string;
};

export type QuestionsCountTypes = {
  categoryId: number;
  difficulty: string;
};
