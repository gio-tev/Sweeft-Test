import {StyleSheet} from 'react-native';

export const getResultsStyles = () => {
  return StyleSheet.create({
    container: {
      flex: 1,
      alignItems: 'center',
      gap: 80,
      backgroundColor: 'white',
    },
  });
};
