import {Text} from 'react-native';

const Title = () => {
  return (
    <Text
      style={{
        color: 'red',
        fontSize: 40,
        marginTop: '30%',
        fontWeight: '700',
      }}>
      Sweeft Test
    </Text>
  );
};

export default Title;
